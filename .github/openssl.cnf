openssl_conf = openssl_init

[openssl_init]
providers = provider_sect

[provider_sect]
fips = fips_sect

[fips_sect]
activate = 1
module = C:/Program Files/OpenSSL/ssl/fipsmodule.cnf

[evp_settings]
fips_mode = on 
